# Copy to .env and fill in before running: docker compose up -d --build
DB_PASSWORD=change-me
# Folder on the server that holds your songs
MUSIC_HOST_DIR=./backend/music
# Port the site is served on
WEB_PORT=80
